"use strict";

const PRODUCTION_MODE_SWITCH_METHODS = Object.freeze(["setMultipleMode", "changeMultipleMode", "switchMultipleMode", "onMultipleModeChange"]);

function productionModeRuntimeHelpersPrelude() {
  return `const multipleModeManager=(runtime?.mManagers||[]).find(manager=>manager?._multipleModeMap instanceof Map),productionModeIdOf=value=>String(value?.modeId??value?.multiple??value?.value??value??"single"),productionModeSwitchMethod=${JSON.stringify(PRODUCTION_MODE_SWITCH_METHODS)}.find(name=>typeof multipleModeManager?.[name]==="function")||null,productionModeCurrentFor=grid=>productionModeIdOf(multipleModeManager?._multipleModeMap?.get?.(String(grid?.itemId))??multipleModeManager?._multipleModeMap?.get?.(Number(grid?.itemId))??multipleModeManager?._multipleModeMap?.get?.(grid?.index)),productionTheoryFor=(grid,modeId)=>{const config=grid?.item?.itemConfig||{},values=Array.isArray(config.CreateData)?config.CreateData.map(String).filter(Boolean):[],counts=new Map();for(const itemId of values)counts.set(itemId,(counts.get(itemId)||0)+1);return values.length?{configVersion:String(config.Version??config.version??config.id??"runtime-current")+":"+values.join(",")+":"+modeId,extractionSource:"grid.item.itemConfig.CreateData",outputsPerAction:modeId==="quad"?4:modeId==="double"?2:1,outcomes:[...counts].map(([itemId,weight])=>({itemId,weight,probability:weight/values.length}))}:null},productionModesFor=grid=>{const modeIds=["single","double",...(multipleModeManager?._isOpenedFourfoldMode?["quad"]:[])],current=productionModeCurrentFor(grid);if(!modeIds.includes(current))modeIds.push(current);return modeIds.map(modeId=>({modeId,unlocked:modeId!=="quad"||!!multipleModeManager?._isOpenedFourfoldMode,theoreticalDistribution:productionTheoryFor(grid,modeId)}))};`;
}

module.exports = { PRODUCTION_MODE_SWITCH_METHODS, productionModeRuntimeHelpersPrelude };
