import React from "react";
import ReactDOM from "react-dom/client";
import CatalogReviewPrototype from "./CatalogReviewPrototype";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <CatalogReviewPrototype />
  </React.StrictMode>,
);
