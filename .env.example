# 适配实验室连接本地微信 CDP 代理
CDP_URL=ws://127.0.0.1:62000
CDP_TIMEOUT_MS=10000
CDP_DISCOVERY_MS=1500

# 微信/WMPF 调试桥端口；对应 wx:cdp 的命令行参数
# npm run wx:cdp -- --debug-port 9421 --cdp-port 62000

# 离线校验 WMPF 模块时可显式指定 flue.dll
# APP_RUNTIME_MODULE=D:\\path\\to\\flue.dll
